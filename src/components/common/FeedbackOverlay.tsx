import React from 'react';

/**
 * Pop up banner overlay disabled per user request.
 * Action completions, tasks, and text outcomes now update stats directly in the HUD
 * without intrusive pop-up banners.
 */
export const FeedbackOverlay: React.FC = () => {
  return null;
};

